# GitHub Pages 部署问题排查指南

## ✅ 已修复的问题

1. **项目结构问题** ✅
   - 原问题：Docusaurus 项目在 `lanbitou/lanbitou` 子目录
   - 已修复：所有文件已移到根目录 `lanbitou/`
   - GitHub Actions 配置已更新

2. **配置文件占位符** ✅
   - 原问题：`docusaurus.config.js` 中有 `YOUR_GITHUB_USERNAME` 占位符
   - 已修复：已替换为 `Xuperbad`

## 📋 部署前检查清单

### 1. GitHub 仓库设置

- [ ] 仓库已创建：`https://github.com/Xuperbad/lanbitou`
- [ ] 仓库是公开的（Public）
- [ ] 已推送代码到 main 分支

### 2. GitHub Pages 设置

进入仓库 Settings → Pages：

- [ ] **Source** 选择：`GitHub Actions`（不是 Deploy from a branch）
- [ ] 保存设置

### 3. 代码提交

需要提交最新的更改：

```bash
# 查看更改
git status

# 添加所有更改
git add .

# 提交
git commit -m "修复部署配置：调整项目结构和更新配置"

# 推送到 GitHub
git push origin main
```

### 4. 查看部署状态

1. 推送后，访问：`https://github.com/Xuperbad/lanbitou/actions`
2. 查看 "Deploy to GitHub Pages" 工作流
3. 等待构建完成（绿色勾号 ✓）

## 🔍 常见问题排查

### 问题 1：Actions 标签页没有工作流运行

**可能原因：**
- GitHub Pages 设置中没有选择 "GitHub Actions"
- `.github/workflows/deploy.yml` 文件不在仓库中

**解决方法：**
1. 确认 Settings → Pages → Source 选择了 "GitHub Actions"
2. 确认 `.github/workflows/deploy.yml` 文件已提交到仓库

### 问题 2：构建失败（红色 ✗）

**排查步骤：**
1. 点击失败的工作流查看详细日志
2. 常见错误：
   - **npm ci 失败**：检查 `package-lock.json` 是否存在
   - **npm run build 失败**：本地运行 `npm run build` 测试
   - **路径错误**：检查 `.github/workflows/deploy.yml` 中的路径

### 问题 3：部署成功但页面显示 404

**可能原因：**
- `baseUrl` 配置错误
- 需要等待几分钟生效

**解决方法：**
1. 检查 `docusaurus.config.js`：
   ```javascript
   url: 'https://Xuperbad.github.io',
   baseUrl: '/lanbitou/',
   ```
2. 等待 5-10 分钟后重试
3. 清除浏览器缓存

### 问题 4：页面样式丢失

**可能原因：**
- `baseUrl` 配置错误

**解决方法：**
确认 `baseUrl: '/lanbitou/'` 与仓库名一致

## 🚀 正确的部署流程

### 第一次部署

```bash
# 1. 确认在项目根目录
cd d:\AppDev\lanbitou

# 2. 添加所有文件
git add .

# 3. 提交
git commit -m "修复部署配置"

# 4. 推送到 GitHub
git push origin main

# 5. 访问 GitHub Actions 查看部署状态
# https://github.com/Xuperbad/lanbitou/actions
```

### 后续更新

```bash
# 1. 修改文件后
git add .
git commit -m "更新内容描述"
git push

# 2. GitHub Actions 会自动部署
```

## 📍 访问地址

部署成功后访问：
```
https://Xuperbad.github.io/lanbitou/
```

## 🧪 本地测试

在推送前，建议本地测试：

```bash
# 构建生产版本
npm run build

# 本地预览构建结果
npm run serve
```

访问 `http://localhost:3000/lanbitou/` 查看效果

## 📞 需要帮助？

如果遇到问题：
1. 查看 GitHub Actions 的详细日志
2. 检查本地是否能成功构建
3. 确认所有配置文件正确

---

**下一步：提交代码并推送到 GitHub！**

